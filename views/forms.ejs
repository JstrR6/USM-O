<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | Military Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body class="bg-gray-100">
    <div class="min-h-screen flex">
        <!-- Sidebar -->
        <%- include('partials/sidebar') %>

        <!-- Main Content -->
        <div class="flex-1 p-6">
            <h1 class="text-2xl font-bold mb-6">Forms</h1>

            <div class="folder-grid">
                <% const rank = user.highestRole; %>

                <% if (rank === 'Citizen') { %>
                    <div class="folder bg-white rounded-lg p-4 shadow cursor-pointer">
                        <i class="fas fa-user text-blue-500 text-2xl mb-2"></i>
                        <p class="font-semibold">Citizen</p>
                    </div>
                <% } %>

                <% if ([
                    'Airman Basic', 'Airman', 'Airman First Class', 'Senior Airman'
                ].includes(rank)) { %>
                    <div class="folder bg-white rounded-lg p-4 shadow cursor-pointer">
                        <i class="fas fa-users text-green-500 text-2xl mb-2"></i>
                        <p class="font-semibold">Enlisted</p>
                    </div>
                <% } %>

                <% if ([
                    'Staff Sergeant', 'Technical Sergeant', 'Master Sergeant', 'First Sergeant'
                ].includes(rank)) { %>
                    <div class="folder bg-white rounded-lg p-4 shadow cursor-pointer">
                        <i class="fas fa-user-shield text-yellow-500 text-2xl mb-2"></i>
                        <p class="font-semibold">NCO</p>
                    </div>
                <% } %>

                <% if ([
                    'Senior Master Sergeant', 'Senior First Sergeant', 'Chief Master Sergeant',
                    'Chief First Sergeant', 'Command Chief Master Sergeant', 'Senior Enlisted Leader',
                    'Chief Senior Enlisted Leader', 'Chief Master Sergeant of the Air Force'
                ].includes(rank)) { %>
                    <div class="folder bg-white rounded-lg p-4 shadow cursor-pointer">
                        <i class="fas fa-user-tie text-purple-500 text-2xl mb-2"></i>
                        <p class="font-semibold">SNCO</p>
                    </div>
                <% } %>

                <% if ([
                    'Second Lieutenant', 'First Lieutenant', 'Captain'
                ].includes(rank)) { %>
                    <div class="folder bg-white rounded-lg p-4 shadow cursor-pointer">
                        <i class="fas fa-chess-pawn text-indigo-500 text-2xl mb-2"></i>
                        <p class="font-semibold">Company Officers</p>
                    </div>
                <% } %>

                <% if ([
                    'Major', 'Lieutenant Colonel', 'Colonel'
                ].includes(rank)) { %>
                    <div class="folder bg-white rounded-lg p-4 shadow cursor-pointer">
                        <i class="fas fa-chess-rook text-indigo-700 text-2xl mb-2"></i>
                        <p class="font-semibold">Field Officers</p>
                    </div>
                <% } %>

                <% if ([
                    'Brigadier General', 'Major General', 'Lieutenant General', 'General', 'General of the Air Force'
                ].includes(rank)) { %>
                    <div class="folder bg-white rounded-lg p-4 shadow cursor-pointer">
                        <i class="fas fa-chess-king text-red-600 text-2xl mb-2"></i>
                        <p class="font-semibold">Headquarters</p>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>